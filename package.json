{
  "name": "chats-viewer",
  "private": true,
  "version": "0.1.0",
  "description": "Local viewer for Claude Code / Cursor / Codex / Grok conversation history",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "cross-env NODE_ENV=production node server/dist/index.js",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
